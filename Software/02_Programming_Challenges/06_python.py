"""
 * Create a program that reverses the order of a string
 * without using built-in language functions that do it automatically.
 * - If we pass "Hello World" it would return "dlroW olleH"
 """

word = input("Introduce one sentence or word ")

print(word[::-1])